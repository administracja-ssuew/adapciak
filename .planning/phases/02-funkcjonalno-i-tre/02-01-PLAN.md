---
phase: 02-funkcjonalno-i-tre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/adapciak/index.html
autonomous: true
requirements:
  - CONT-01
  - CONT-02

must_haves:
  truths:
    - "Meta description tag contains the string '30.08–06.09.2026'"
    - "og:title tag contains the string 'Adapciak 2026 (30.08–06.09)'"
    - "og:description tag contains the string '30.08–06.09.2026'"
    - "Each of the three social media links (Facebook, Instagram, TikTok) has an HTML comment immediately before it indicating where to insert the URL"
  artifacts:
    - path: "C:/adapciak/index.html"
      provides: "Updated meta tags and social media placeholder comments"
      contains: "30.08–06.09.2026"
  key_links:
    - from: "index.html head"
      to: "meta[name=description]"
      via: "content attribute"
      pattern: "30\\.08–06\\.09\\.2026"
    - from: "index.html #kontakt"
      to: "social media anchor tags"
      via: "HTML comments"
      pattern: "WSTAW URL"
---

<objective>
Update meta tags with the 2026 event date and add per-link HTML placeholder comments to all three social media links.

Purpose: Makes the page shareable with correct Open Graph previews and flags the three social URLs that need real links before go-live — zero user-facing visual change.
Output: index.html with corrected meta content and comment-annotated social links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/adapciak/.planning/ROADMAP.md
@C:/adapciak/.planning/phases/02-funkcjonalno-i-tre/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update meta tags with 2026 date (CONT-01)</name>
  <files>C:/adapciak/index.html</files>

  <read_first>
    C:/adapciak/index.html — read lines 1–15 to see current meta tag content before editing
  </read_first>

  <action>
Replace the three meta tags on lines 6–8 of index.html with the following exact content. Do not change any other attributes (charset, viewport, og:type, og:image) — only the three lines below:

Line 6 — replace:
```html
<meta name="description" content="Adapciak – flagowe wydarzenie Samorządu Studentów UEW we Wrocławiu. Dołącz do największej studenckiej imprezy integracyjnej!"/>
```
with:
```html
<meta name="description" content="Adapciak 30.08–06.09.2026 – flagowe wydarzenie integracyjne Samorządu Studentów UEW we Wrocławiu. Dołącz do największej studenckiej imprezy!"/>
```

Line 7 — replace:
```html
<meta property="og:title" content="Adapciak – SSUEW"/>
```
with:
```html
<meta property="og:title" content="Adapciak 2026 (30.08–06.09) – SSUEW"/>
```

Line 8 — replace:
```html
<meta property="og:description" content="Największa studencka impreza integracyjna UEW we Wrocławiu."/>
```
with:
```html
<meta property="og:description" content="Największa studencka impreza integracyjna UEW we Wrocławiu. Adapciak 30.08–06.09.2026 – bądź tam!"/>
```
  </action>

  <verify>
    <automated>
grep -n "30\.08" C:/adapciak/index.html | head -10
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep 'name="description"' C:/adapciak/index.html` returns a line containing both `Adapciak 30.08–06.09.2026` and `flagowe wydarzenie integracyjne`
    - `grep 'og:title' C:/adapciak/index.html` returns a line containing `Adapciak 2026 (30.08–06.09) – SSUEW`
    - `grep 'og:description' C:/adapciak/index.html` returns a line containing `30.08–06.09.2026 – bądź tam!`
    - Total count of meta tags in head is unchanged — no meta tags deleted or duplicated
  </acceptance_criteria>

  <done>All three meta tags contain the 2026 date string "30.08–06.09.2026"; og:title contains "Adapciak 2026 (30.08–06.09) – SSUEW"</done>
</task>

<task type="auto">
  <name>Task 2: Add per-link HTML placeholder comments to social media links (CONT-02)</name>
  <files>C:/adapciak/index.html</files>

  <read_first>
    C:/adapciak/index.html — read lines 650–670 to see the current social media link block and the existing generic comment before editing
  </read_first>

  <action>
In the social media link block (currently around lines 654–664), replace the single generic comment with three individual comments — one immediately before each anchor tag. The existing generic comment on line 655 (`<!-- ← podmień # na prawdziwe URL mediów społecznościowych -->`) should be removed and replaced with the three per-link comments below.

The block should look exactly like this after editing:

```html
<div class="flex gap-3 flex-wrap">
  <!-- WSTAW URL Facebook: https://facebook.com/... -->
  <a href="#" class="border border-surface-border text-secondary hover:border-primary hover:text-primary transition-colors font-label-bold text-label-bold uppercase px-5 py-3 flex items-center gap-2">
    <span class="material-symbols-outlined text-[16px]" aria-hidden="true">group</span> Facebook
  </a>
  <!-- WSTAW URL Instagram: https://instagram.com/... -->
  <a href="#" class="border border-surface-border text-secondary hover:border-primary hover:text-primary transition-colors font-label-bold text-label-bold uppercase px-5 py-3 flex items-center gap-2">
    <span class="material-symbols-outlined text-[16px]" aria-hidden="true">photo_camera</span> Instagram
  </a>
  <!-- WSTAW URL TikTok: https://tiktok.com/... -->
  <a href="#" class="border border-surface-border text-secondary hover:border-primary hover:text-primary transition-colors font-label-bold text-label-bold uppercase px-5 py-3 flex items-center gap-2">
    <span class="material-symbols-outlined text-[16px]" aria-hidden="true">music_video</span> TikTok
  </a>
</div>
```

Do not change any class attributes or the `href="#"` values. Only add the three comments and remove the old generic one.
  </action>

  <verify>
    <automated>
grep -n "WSTAW URL" C:/adapciak/index.html
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "WSTAW URL" C:/adapciak/index.html` outputs `3` (exactly 3 matches)
    - `grep "WSTAW URL Facebook" C:/adapciak/index.html` returns a line containing `WSTAW URL Facebook: https://facebook.com/...`
    - `grep "WSTAW URL Instagram" C:/adapciak/index.html` returns a line containing `WSTAW URL Instagram: https://instagram.com/...`
    - `grep "WSTAW URL TikTok" C:/adapciak/index.html` returns a line containing `WSTAW URL TikTok: https://tiktok.com/...`
    - The old generic comment `podmień # na prawdziwe URL` is no longer present (grep returns no match)
    - All three anchor tags still have `href="#"` unchanged
  </acceptance_criteria>

  <done>Each of the three social media links (Facebook, Instagram, TikTok) has a dedicated HTML comment immediately before it with the pattern "WSTAW URL {Platform}: https://..."</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Open index.html in a browser and view page source — confirm meta description shows "30.08–06.09.2026"
2. Confirm og:title shows "Adapciak 2026 (30.08–06.09) – SSUEW" in source
3. Search page source for "WSTAW URL" — must appear 3 times, once before each social link
4. Visual page appearance unchanged — social buttons still render as before
</verification>

<success_criteria>
- `grep -c "30\.08–06\.09\.2026" C:/adapciak/index.html` returns at least 3 (meta description, og:description, og:title)
- `grep -c "WSTAW URL" C:/adapciak/index.html` returns exactly 3
- Page loads without errors in browser
</success_criteria>

<output>
After completion, create `C:/adapciak/.planning/phases/02-funkcjonalno-i-tre/02-01-SUMMARY.md`
</output>
